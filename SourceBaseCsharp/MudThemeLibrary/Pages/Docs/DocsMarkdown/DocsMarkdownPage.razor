@page "/docs/markdown"
<PageTitle>Docs | Markdown</PageTitle>

<_DocsLayout>
    <MudGrid>
        <MudItem xs="12">
            <MudMarkdown Value="@_valueMarkdown" />
        </MudItem>
        <MudItem xs="12">
            <MudMarkdown Value="@_valueMarkdownExample" />
        </MudItem>
    </MudGrid>
</_DocsLayout>

@code{
    private string _valueMarkdown { get; } = @"
# 🎉 Giới thiệu Markdown trong MudBlazor

Đây là một ví dụ **Markdown** sử dụng trong `MudBlazor.Markdown` để hiển thị nội dung đẹp mắt trong ứng dụng Blazor.

---

## 📋 Bảng Thông Tin

| Tên       | Vai Trò         | Ghi chú           |
|-----------|------------------|-------------------|
| Alice     | Quản trị viên    | Có quyền cao nhất |
| Bob       | Người dùng       | Quyền hạn giới hạn |
| Charlie   | Khách truy cập   | Chỉ xem được nội dung |

---

## ✅ Danh sách công việc

### Việc cần làm:
- [x] Cài đặt MudBlazor
- [x] Cài đặt MudBlazor.Markdown
- [ ] Tạo component hiển thị Markdown
- [ ] Tùy chỉnh style Markdown

---

## 🔢 Danh sách có thứ tự

1. Mở Visual Studio
2. Tạo project Blazor WebAssembly Standalone
3. Cài package `MudBlazor.Markdown`
4. Hiển thị Markdown

---

## 💬 Trích dẫn (Blockquote)

> ""Markdown giúp viết nội dung dễ dàng hơn.""
> — Một lập trình viên vui tính 😄

---

## 🧑‍💻 Đoạn mã (Code block)

### C# Example:
```csharp
@code {
    string message = ""Hello Markdown!"";
    void Show() => Console.WriteLine(message);
}
";

    private string _valueMarkdownExample { get; } = @"
##### Markdown Example:
``` csharp
<MudMarkdown Value=""@_valueMarkdown"" />

@code {
        private string _valueMarkdown { get; } = @""
# 🎉 Giới thiệu Markdown trong MudBlazor

Đây là một ví dụ **Markdown** sử dụng trong `MudBlazor.Markdown` để hiển thị nội dung đẹp mắt trong ứng dụng Blazor.

---

## 📋 Bảng Thông Tin

| Tên       | Vai Trò         | Ghi chú           |
|-----------|------------------|-------------------|
| Alice     | Quản trị viên    | Có quyền cao nhất |
| Bob       | Người dùng       | Quyền hạn giới hạn |
| Charlie   | Khách truy cập   | Chỉ xem được nội dung |

---

## ✅ Danh sách công việc

### Việc cần làm:
- [x] Cài đặt MudBlazor
- [x] Cài đặt MudBlazor.Markdown
- [ ] Tạo component hiển thị Markdown
- [ ] Tùy chỉnh style Markdown

---

## 🔢 Danh sách có thứ tự

1. Mở Visual Studio
2. Tạo project Blazor WebAssembly Standalone
3. Cài package `MudBlazor.Markdown`
4. Hiển thị Markdown

---

## 💬 Trích dẫn (Blockquote)

> ""Markdown giúp viết nội dung dễ dàng hơn.""
> — Một lập trình viên vui tính 😄

---

## 🧑‍💻 Đoạn mã (Code block)

### C# Example:
```csharp
@code {
    string message = ""Hello Markdown!"";
    void Show() => Console.WriteLine(message);
}
"";
}
```
";
}